/*package sem_4.pract_2;

import java.util.Comparator;

public class AgeMultWeightComparator implements Comparator<Human> {
    public int compare(Human a, Human b){
        if (a.getAge()*a.getWeight() > b.getAge()*b.getWeight())
            return 1;
        else if (a.getAge()*a.getWeight() < b.getAge()*b.getWeight())
            return 0;
        else
            return -1;
    }
}
*/


/*implements Comparable<Human>*/


// public int compareTo(Human other){
// return Character.compare(this.firstName.charAt(1), other.firstName.charAt(1));
//}



//AgeMultWeightComparator comp = new AgeMultWeightComparator();